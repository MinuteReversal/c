#include <stdio.h>

//The type unsigned char is often used to represent a byte, which is not a built-in type in C++
int main(int argc, char const *argv[])
{
    unsigned char c=97;
    printf("%s\n", c);
    return 0;
}